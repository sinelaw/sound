# generated audio
*.wav
*.mp3
*.MP3
*.midi
*.mid
stems/
# ...but the band arrangement MIDI is the tracked "score" (source of truth
# for the notes; edit in a DAW). The base transcription MIDI is regenerable
# from the .ly, so it stays ignored.
!uf-gozal-band.midi

# generated engravings (but keep the original input score)
*.pdf
!oof-orig.pdf

# python env / caches (regenerate with: cd omr && uv sync)
omr/.venv/
__pycache__/
*.pyc

# downloaded sample libraries (multi-GB, fetched separately)
sample-libraries/

# OMR working data / scratch
unzipped/
old/
*.log
*.png
ardour-test*/

# large source inputs kept out of git
oof.zip

# generated score intermediates (music21 lilypond source + pdf)
uf-gozal-score
uf-gozal-score.ly
